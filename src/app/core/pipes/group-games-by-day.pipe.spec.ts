import { GroupGamesByDayPipe } from './group-games-by-day.pipe';

describe('GroupGamesByDayPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupGamesByDayPipe();
    expect(pipe).toBeTruthy();
  });
});
